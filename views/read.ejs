<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com/3.4.16"></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/locomotive-scroll@4.1.4/dist/locomotive-scroll.min.css"
    />
  </head>
  <body>
    <div
      class="w-full min-h-full bg-zinc-900 p-10 scroll-container"
      data-scroll-container
    >
      <a
        href="/"
        class="text-blue-500 tracking-tighter"
        >Go to Home</a
      >

      <h1 class="text-5xl font-bold text-white mt-10 tracking-tighter mb-5">
        All Users
      </h1>
      <hr />

      <div class="flex flex-wrap gap-4">

        <% if (users.length === 0) { %>
        <p class=" mt-10 text-zinc-700">No users found.</p>
        <%} else {%>

        <% users.forEach((user) => { %>
        <div class="bg-zinc-800 p-5 rounded-md mt-5 w-56 h-fit">
          <img
            src="<%= user.image %>"
            alt="<%= user.name %>'s image"
            class="w-full h-72 rounded-sm mb-5 object-cover"
          />
          <h2 class="text-xl font-bold text-white tracking-tighter">
            <%= user.name %>
          </h2>
          <p class="text-gray-400 tracking-tighter"><%= user.email %></p>

          <a href="/delete/<%=user._id%>" class="mt-5 text-red-500 inline-block">Delete</a>
          <a href="/update/<%=user._id%>" class="mt-5 text-zinc-500 inline-block ml-2">Edit User</a>
        </div>
        <%}) %>

        <%}%>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/locomotive-scroll@4.1.4/dist/locomotive-scroll.min.js"></script>
    <script>
      const scroll = new LocomotiveScroll({
        el: document.querySelector("[data-scroll-container]"),
        smooth: true,
      });
    </script>
  </body>
</html>
